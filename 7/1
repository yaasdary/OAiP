#include <iostream>
#include <string>
#include <cstdio>
 
using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	int k;
	char str[100];
	cout << "Введите строку: ";
	fgets(str, sizeof(str), stdin);
	k = strlen(str) - 1;
	cout << "Длина строки: " << k;
	return 0;
}
