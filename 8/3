#include <iostream>
#include <cmath>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	int N, n = 0;
	cout << "N: ";
	cin >> N;
	while (N > 0) {
		n = n * 10 + (N % 10);
		N /= 10;
	}
	while (n > 0) {
		cout << n % 10 << endl;
		n /= 10;
	}
	return 0;
}
